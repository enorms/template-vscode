Modify: 
  tasks.json > tasks > args: file names
  launch.json > configurations > program

C++.gitignore > .gitignore

Run:
  build (shift-cmd-B)
  run 
    debugging (stops at first line of main) (F5)
    without debugging (ctr-F5)
      We don't support launch without debugging through the UI at this time.